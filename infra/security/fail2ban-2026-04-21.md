# fail2ban install check - 2026-04-21

Host checked: `root@100.79.2.56`

## Actions performed

```bash
apt-get update
apt-get install -y fail2ban
install -d /etc/fail2ban
install -m 644 /tmp/jail.local /etc/fail2ban/jail.local
systemctl enable --now fail2ban
fail2ban-client status sshd
systemctl is-active fail2ban
```

## Result

- `fail2ban` installed successfully
- `sshd` jail is active
- service state: `active`

At verification time:

- current failed attempts: `1`
- total failed attempts: `45`
- currently banned IPs: `7`

The deployed config comes from `infra/fail2ban/jail.local`.
