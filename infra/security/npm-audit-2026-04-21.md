# npm audit review — 2026-04-21

Command run:

```bash
npm audit --production --json > tmp/audit.json
```

Result summary:

- `high`: `0`
- `critical`: `0`
- `moderate`: `6`

Gate decision:

- No `high` or `critical` production findings were reported by `npm audit`.
- Because the filtered result set is empty, there is no package diff to propose for this security-review item.
- I did **not** run `npm audit fix`; doing that here would be cargo-culting, not remediation.

Notes for manual follow-up if you want to reduce the moderate-only tail later:

- `hono@4.12.10` and `@hono/node-server@1.19.12` come via `shadcn -> @modelcontextprotocol/sdk`.
- `drizzle-kit@0.31.10` is flagged through the `@esbuild-kit/* -> esbuild` advisory chain.
- The audit output suggested a `drizzle-kit` "fix" to `0.18.1`, which is lower than the currently installed `0.31.10`; that recommendation is not trustworthy enough to apply blindly.

Suggested next step for moderate-only cleanup:

```diff
# No diff proposed for 1.9 because npm audit reported 0 high / 0 critical findings.
```
