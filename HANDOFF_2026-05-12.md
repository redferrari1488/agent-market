# Handoff — 2026-05-12 (мак-сессия: фидбек друга по UX, 9 из 14 в проде)

Короткая мак-сессия после возврата с PC. Главное: друг надиктовал 14 голосовых замечаний по сайту, прошлись по каждому, закрыли 9 в проде (`bb7ccb5`). Заодно починили устаревшую запись в памяти про BYOK — у нас managed via OpenRouter, и обновили `CLAUDE.md`.

**Prod HEAD: `bb7ccb5`. Phase 0: 6/7 закрыто (#6 биржа заказов).**

---

## 1. Что сделано

### Обсуждение друга (разбор 14 пунктов)

Друг прислал голосовые с фидбеком, прошлись по каждому. Не записывали в todo автоматически (`feedback_no_premature_tasks`), сначала разобрали что валидно / что мимо / что противоречит Phase 0, потом юзер дал зелёный свет на конкретные пункты.

### Коммит `bb7ccb5` — ux(landing+seller+catalog): фидбек друга — конверсия, видео, дизайн-ритм

**Закрыто 9 из 14:**

| # | Что | Где |
|---|---|---|
| 3 | NBSP перед em-dash в «От выбора до запуска» | `FlowCinematic.tsx` |
| 4 | Заголовки секций → единый размер `clamp(36px,_,48px)` + `max-w-2xl` | `LandingAnimations.tsx`, `FlowCinematic.tsx` |
| 5 | «Стать продавцом» — cyan-bordered кнопка вместо тихих брекетов | `LandingAnimations.tsx` |
| 6 | Все секции лендинга `py-32` + `border-t` (единый ритм) | `LandingAnimations.tsx`, `FlowCinematic.tsx` |
| 7 | `BackgroundVideo` с auto-resume (pause/stalled/suspend/visibilitychange) + `preload=auto` | `LandingAnimations.tsx` |
| 8 | «Войти» в шапке — cyan-bordered кнопка | `Header.tsx` |
| 9 | `/agents`: убран AgentSplash, каталог сразу | `AgentCatalogClient.tsx` |
| 10 | `/seller`: доступна без логина, форма → «войти и подать заявку» | `seller/page.tsx`, `BecomeSellerLanding.tsx`, `proxy.ts` |
| 12 | HeroCockpit: «остановить/рестарт/настройки» + «↵ новый» — Link → `/agents` | `HeroCockpit.tsx` |

**Бонусом:**
- `CLAUDE.md`: AI tokens — managed via OpenRouter (не BYOK). Раньше там было «BYOK only», что не соответствовало реальной архитектуре
- Память `project_managed_keys_post_phase0.md` обновлена: managed уже работает в проде через OpenRouter, не «будущая идея»
- `proxy.ts`: CSP отключён в development (Turbopack nonce mismatch ломал локальные стили). В проде CSP работает как раньше

### Бэкап перед сессией
Tag `backup/pre-friend-feedback-2026-05-12` указывает на `47d9cbb`. Откат всей сессии: `git revert bb7ccb5 && git push && ssh root@77.239.104.149 'cd /opt/agent-market && git pull && docker compose up -d --build app'`.

---

## 2. Не сделано из 14 (требуют решения / уточнения)

- **#1, #11 — мелкий шрифт + яркость/шрифты:** системно по типографике, попадёт само при редизайне карточки агента (если редизайн будет). Юзер сказал «не вижу смысла фиксить карточку агента» — пункты сами отвалились на этой сессии, но в редизайне карточки следить
- **#2 — «убрать упоминания bot token/API»:** юзер сказал «погнали», но конкретных мест не назвал. На лендинге их нет — это в формах настройки агента (`/dashboard/agents/[id]/edit`, `SetupSchemaBuilder.tsx`, `AgentForm.tsx`). Нужны конкретные скрины
- **#13 — логотип h → h.ireon:** юзер сказал «обсудим» — обсуждение отложено
- **#14 — «темно, важной инфы сразу нет, непонятно куда ведут»:** частично закрыто через 5/8/9/10 (понятная навигация, видимые CTA). Полностью — требует рерайт hero-копи + решение по тёмной теме (это вкус)

---

## 3. Дизайн-флоу (carryover из HANDOFF_2026-05-02)

Контракт по дизайну (юзер итерирует в Claude Design до «вау», я переношу 1-в-1 без подгонки под токены) — в силе.

**Приоритеты, что осталось:**

1. **Agent Page (`/agents/[slug]`)** — ✅ перенесён в PC-сессии прошлой недели («01 · spec sheet», `47d9cbb`), доступен на проде
2. **Карточка агента (атом каталога `/agents`)** — следующая. Юзер сказал на этой сессии «после неё фиксируем визуальный язык в `instructions/design.md`, и логин + dashboard уже под этот язык»
3. **Логин (`/auth/login`)** — после карточки, с привязкой к языку
4. **Dashboard внутренний (`/dashboard/agents/[id]`)** — последним

Промты для (3) и (4) были подготовлены вчера на PC, но юзер их хранит сам. Их НЕЛЬЗЯ использовать как есть — надо переписать под зафиксированный язык карточки. Если нужны — попроси юзера восстановить из транскрипта.

---

## 4. Phase 0 — статус

| # | Задача | Статус |
|---|---|---|
| 1 | Юр.страницы + waitlist | ✅ |
| 2 | Pivot на Gemini-модель | ✅ |
| 3 | Форма заявки продавца + админ-очередь | ✅ |
| 4 | Outbound-ссылка на карточке стороннего агента | ✅ |
| 5 | Founder-sold чекаут end-to-end | ✅ |
| 6 | Биржа заказов (репрофилирование access_requests) | ⏳ не начато |
| 7 | Хэндофф | ✅ |

**#6 — биржа заказов:** репрофилировать `access_requests` → `orders`, миграция + 2-3 страницы (форма создания заказа, лента заказов, отклики продавцов) + админ-модерация. Большой кусок.

---

## 5. Тех.долги до прода и рекламы

- `PAYMENTS_ALLOW_CRYPTOMUS` не пробрасывается в `docker-compose.yml`. Фикс: `PAYMENTS_ALLOW_CRYPTOMUS: ${PAYMENTS_ALLOW_CRYPTOMUS:-}` в секции `environment` контейнера `app`
- `compute_class` пересмотр. У `news-digest-bot` total = 3190₽/мес — дорого. Теперь когда подтвердили что Hireon-агенты через OpenRouter (токены дешевле чем direct Anthropic/OpenAI) — есть основание пересчитать L-класс или вынести S-класс
- Resend (email-уведомления) — внешний блокер с 2026-04-21
- Cron `yookassa-recurring` не тестирован end-to-end (нужна реальная подписка 30+ дней)
- Смок-тест ЮКассы боевой картой (~1500₽ один раз)
- Удалить тестовую запись `test-external-agent`:
  ```bash
  ssh root@77.239.104.149 'docker compose -f /opt/agent-market/docker-compose.yml exec -T postgres psql -U agentmarket -d agentmarket -c "DELETE FROM agents WHERE slug='"'"'test-external-agent'"'"';"'
  ```

---

## 6. Память: что синхронизировать

На маке в `~/.claude/projects/-Users-monkmode-agent-market/memory/` — 3 записи: `feedback_no_premature_tasks`, `project_pre_launch_strategy`, `project_managed_keys_post_phase0` (последний обновлён в этой сессии — теперь про OpenRouter managed).

По прошлому промту юзера ожидалось 6 записей. Недостающие 3: `user_profile`, `project_architecture`, `feedback_work_mode` — остались на PC. `.claude-memory.tar.gz` в корне (от 11 мая 18:30) — это экспорт мак-стороны, не PC-стороны, поэтому при `scripts/memory-sync.sh import` мы получаем те же 3.

**Решение:** при следующем заходе на PC сделать `scripts/memory-sync.sh export`, перекинуть архив на мак, импортировать.

---

## 7. .env.local на маке

`.env.local` отсутствует — я создал минимальный с заглушками (рандомные `BETTER_AUTH_SECRET`/`ENCRYPTION_KEY`, без LIVE-секретов) чтобы билд проходил. Для реальной работы (БД, чекаут) нужен рабочий `.env.local` с PC: принеси через iCloud / Telegram себе / scp.

Локальный postgres не запущен (Docker не установлен, brew postgres тоже нет). Если в следующей сессии понадобится локальная БД — `brew install postgresql@16 && brew services start postgresql@16 && psql -f db/migration.sql && psql -f db/seed.sql`.

---

## 8. Ключевые константы

- prod HEAD: `bb7ccb5`
- backup tag перед фидбеком друга: `backup/pre-friend-feedback-2026-05-12` (`47d9cbb`)
- ЮКасса live: `shop_id=1334693`
- ИНН Родимова: `615520487706`
- VPS: tailscale `100.79.2.56` (public `77.239.104.149` fallback, если тэйлскейл выключен)
- Триггер ИП на УСН 6%: 5+ продавцов + 20+ заявок ИЛИ >150к/мес
- Deploy: `git push && ssh root@77.239.104.149 'cd /opt/agent-market && git pull && docker compose up -d --build app'`

---

## 9. Промт для следующей сессии (копипаст)

```
Привет. Продолжаем Hireon. Прочитай HANDOFF_2026-05-12.md в корне
(он ссылается на HANDOFF_2026-05-02.md для большого контекста).

ПЕРЕД РАБОТОЙ:
- git pull (или startproj) — должен догнать до prod HEAD bb7ccb5
- Проверь ~/.claude/projects/-Users-monkmode-agent-market/memory/ —
  должно быть 6 записей в MEMORY.md (если 3 — синкни с PC через
  scripts/memory-sync.sh либо попроси прислать недостающие
  user_profile / project_architecture / feedback_work_mode)
- .env.local на маке — заглушки. Для реальной работы с БД/чекаутом
  нужен рабочий .env.local с PC

ВАЖНО ПО АРХИТЕКТУРЕ:
- Hireon-агенты ходят через ПЛАТФОРМЕННЫЙ OpenRouter-ключ (managed, не
  BYOK). Не предлагать в UX «вставьте API key»

ПЕРВЫЙ ВОПРОС: с чего начнём?
(а) Доделать оставшиеся пункты фидбека друга (#2, #13, #14) — нужны
    конкретные скрины / решения от меня
(б) Карточка агента через Claude Design (жду handoff)
(в) Phase 0 #6 биржа заказов
(г) Тех.долги (compute_class пересмотр под OpenRouter / docker-compose
    флаг / смок-тест ЮКассы)

КРИТИЧНЫЕ КОНСТАНТЫ:
- ЮКасса live: shop_id=1334693
- ИНН Родимова: 615520487706
- VPS: tailscale 100.79.2.56 (public 77.239.104.149 fallback)
- prod HEAD на 2026-05-12: bb7ccb5
- backup tag: backup/pre-friend-feedback-2026-05-12 (47d9cbb)
- Триггер ИП на УСН 6%: 5+ продавцов + 20+ заявок ИЛИ >150к/мес
- Дизайн-контракт: юзер итерирует в Claude Design до «вау», шлёт
  handoff zip + скрин/видео hover'а. Я переношу один-в-один БЕЗ
  подгонки под токены (если макет требует ярко-оранжевую рамку и
  3D-tilt — будет так)
- Не превращать обсуждения в задачи (feedback_no_premature_tasks)
```
